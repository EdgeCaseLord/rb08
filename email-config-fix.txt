# CORRECTED EMAIL CONFIGURATION FOR PRODUCTION
# Server admin confirmed: No authentication needed, no TLS required

MAIL_MAILER=smtp
MAIL_HOST=ifmmvz.mail.protection.outlook.com
MAIL_PORT=25
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="no-reply@myintest-rezepte.de"
MAIL_FROM_NAME="MyIntest Rezepte"

# ISSUES FOUND AND FIXED:
# 1. MAIL_SCHEME=null - REMOVED (not needed)
# 2. MAIL_PORT=25 - KEPT (server admin confirmed this is correct)
# 3. MAIL_USERNAME=[MAIL_USERNAME] - SET to null (no auth needed)
# 4. MAIL_PASSWORD=[MAIL_PASSWORD] - SET to null (no auth needed)
# 5. MAIL_ENCRYPTION=null - No encryption needed per server admin

# FOR TESTING (use log driver to see emails in storage/logs/email.log):
# MAIL_MAILER=log
